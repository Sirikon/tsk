version: 2

jobs:

  build:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run: sudo apt-get install -y rpm
      - run: go get
      - run: ./scripts/test.sh
      - run: ./scripts/release.sh
      - store_artifacts:
          path: ./dist
          destination: build
